import { FC } from "react";

const App: FC = () => {
  return (
    <form>
      <label>Add language:</label>
      <input type="text" />
      <button>submit</button>
    </form>
  );
};

export default App;
